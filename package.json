{
  "name": "readable-client",
  "private": true,
  "version": "0.0.0",
  "description": "Client app for Readale API",
  "repository": "https://github.com/pubsy/readable-client",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "./node_modules/bower/bin/bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
